#include<bits/stdc++.h>
using namespace std;
int main()
{
	int t;
	cin>>t;
	while(t--)
	{
		int n;
		cin>>n;
		int long long res;
		int a[100005]={0};
		for(int i=0;i<n;i++)
		{
			cin>>a[i];
			if(i==0)
				res=2*a[i];
			else
				res=res^(2*a[i]);
		}
		cout<<res<<endl;
	}
}
