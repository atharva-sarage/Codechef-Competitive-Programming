int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int long long a,b,sum=0,i=0;
		scanf("%lld %lld",&a,&b);
		while(a||b>=1)
		{
		
		sum=sum+((a+b))*pow(10,i);
	
		a=a/10;
		b=b/10;
		
		i++;
	}
	printf("%d\n",sum);
	}
}