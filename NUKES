#include<stdio.h>
int main()
{
	int long long a;
	int k,n,i;
	scanf("%lld %d %d",&a,&n,&k);

	for(i=0;i<k;i++)
	{
		if (a>=1)
		{
			printf("%lld ",a%(n+1));
		a=a/(n+1) ;
		
		}
		else
		{
			printf("0\n");
		}
		
	}
}